关卡编辑器总体设计

1. 创建关卡
创建关卡系统自动分配一个ID（与用户名也无关）XXXXXX，0-9，应该够用了，这个是关卡的唯一识别代码。先从100001开始分配吧。先实现本地保存，以后做网站的时候再考虑网络怎么保存。
创建关卡的时候需要给一个名字，这个名字允许冲突。（关卡用ID区分）

关卡要输入宽和高，最小16x16，最大512x512。另外关卡要输入视窗大小，现在我们设置的视窗是23x15，允许以后有不同于这个尺寸视窗的关卡

